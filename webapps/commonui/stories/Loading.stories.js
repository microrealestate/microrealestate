import { Loading } from '@microrealestate/commonui/components';

const page = {
  title: 'Loading',
  component: Loading,
};

export default page;

export const Default = () => {
  return <Loading />;
};
