# Dev Environment Reminder

For this project, load and run the development environment with:

yarn dev

Do not start the full Docker microservices stack by default for normal development.

Only use Docker-based startup/build/run commands when explicitly requested.
